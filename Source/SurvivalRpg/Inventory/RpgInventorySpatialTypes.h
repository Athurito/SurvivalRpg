#pragma once

// Compatibility include: spatial types now live with persistent graph identity and transaction DTOs so placements
// can carry FRpgInventoryContainerHandle without a reflected-header include cycle.
#include "RpgInventoryGraphTypes.h"
